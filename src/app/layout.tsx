// Root layout — minimal shell required by Next.js App Router.
// HTML/body/lang are set in [locale]/layout.tsx.
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
